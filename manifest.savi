:manifest lib JSON
  :sources "src/*.savi"

:manifest bin "spec"
  :copies JSON
  :sources "spec/*.savi"

  :dependency Spec v0
    :from "github:savi-lang/Spec"
    :depends on Map

  :transitive dependency Map v0
    :from "github:savi-lang/Map"
